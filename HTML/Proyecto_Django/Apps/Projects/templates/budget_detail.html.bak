{% extends 'base.html' %}
{% load static %}`r`n{% load humanize %}

{% block content %}
    <div class="d-flex justify-content-center mt-4">
        <div class="card" style="width: 90%; max-width: 1000px;">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Detalle de presupuesto</h5>
                <a href="{% url 'budget_update' presupuesto.id_presupuesto %}" class="btn btn-warning">
                    <i class="bx bx-edit"></i> Editar
                </a>
            </div>
            <div class="card-body">
                <table class="table table-borderless">
                    <tr>
                        <th width="200">ID:</th>
                        <td>{{ presupuesto.id_presupuesto }}</td>
                    </tr>
                    <tr>
                        <th>Proyecto:</th>
                        <td>{{ presupuesto.id_proyecto.nombre_proyecto }}</td>
                    </tr>
                    <tr>
                        <th>Monto total:</th>
                        <td>Q{{ presupuesto.monto_total|floatformat:2|intcomma }}</td>
                    </tr>
                    <tr>
                        <th>DescripciÃ³n:</th>
                        <td>{{ presupuesto.descripcion|default:"-" }}</td>
                    </tr>
                </table>
                <a href="{% url 'projectsapp' %}" class="btn btn-secondary">Volver</a>
            </div>
        </div>
    </div>
{% endblock %}


